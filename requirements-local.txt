# Chess AI — Python dependencies
#
# Install PyTorch first using the wheel index for your CPU/GPU platform:
#   https://pytorch.org/get-started/locally/
#
# Then install everything else:
#   pip install -r requirements-local.txt

# Deep learning
torch>=2.2.0

# Chess
chess>=1.10.0

# Data & numerics
numpy>=1.26.0
zstandard>=0.22.0
pyarrow>=15.0.0
huggingface_hub>=1.0.0

# Web backend (for GitHub Pages API)
flask>=3.0.0
flask-cors>=4.0.0

# Chess UI (main.py desktop app)
pygame>=2.5.0
Pillow>=10.0.0

# Testing
gradescope-utils>=0.5.0
pytest>=8.0.0
