# Lightweight requirements for Railway deployment.
# Only what app.py actually needs to run Flask + alphabeta.
# PyTorch (for Magnus) is excluded — too large for Railway free tier.
# Magnus will show as unavailable in the UI until a GPU server is used.

flask>=3.0.0
flask-cors>=4.0.0
chess>=1.10.0
numpy>=1.26.0